your-project/
│
├── src/				# reusable library code (functions, no hard-coded paths)
│   ├── __init__.py
│   ├── vape_price_index.py        		# core pipeline logic (library code) that defines refactored transformation functions
│   ├── schemas.py                 		# Pandera schemas (specifies data contracts for dataframes)
│   └── validation.py              		# wrapper functions to validate DataFrames (functions that check if schemas are are adhered to)
│
├── tests/			# small synthetic examples for each new step
│   ├── __init__.py                		# optional, but good practice
│   ├── test_vape_index.py         		# unit tests for functions & pipeline pieces
│   ├── utils.py                   		# creates a tiny synthetic test dataframe (used for unit tests)
│   └── data/
│       ├── sample_store.feather   		# optional sample test file
│       └── sample_index.feather   		# optional sample test file
│
├── data/
│   ├── raw/             			 		# empty placeholder for raw csv files from data provider
│   ├── interim/					 
│       └── da_store_id_monthly_ag/		# per-store .feather input files
│   └── processed/
│       ├── store_vape_price_indexes/     	# store-level price index outputs
│       └── index_panels/vape_price_indexes.feather  # final panel (concatenated store-level indexes)
│
├── scripts/			# entry points / orchestration
│   ├── __init__.py
│   └── run_vape_price_index.py           	# runs the vape_price_index.py library
│   └── validate_data.py           		# CLI used in CI for data-quality checks. A tiny script to validate sample feather files stored under tests/data/
│
├── run_dryrun_10.bat            			# runs pipeline on first N stores
├── run_production.bat           			# full production run (all stores + panel)
├── requirements.txt               		# dependencies
├── pyproject.toml / setup.cfg     		# TO DO: optional but recommended for packaging
│
└── .github/								# TO DO: CI: install deps + run pytest
    └── workflows/
        └── ci.yml                 		# TO DO: GitHub Actions config
